<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SteganographyInPicture.UI.Pages.View.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteganographyInPicture.UI.Pages.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Spacing="10" Margin="10" HorizontalAlignment="Stretch">
                <controls:SettingsCard ContentAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Spacing="10" HorizontalAlignment="Left" Orientation="Vertical">
                            
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="Путь к HTML файлу для блока обучения: " VerticalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.PathToStudyHtml, Mode=OneWay}" VerticalAlignment="Center" ToolTipService.ToolTip="{x:Bind ViewModel.PathToStudyHtml, Mode=OneWay}"/>
                            </StackPanel>

                            <Button Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SetPathToStudyHtmlCommand}">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <SymbolIcon Symbol="OpenFile"/>
                                    <TextBlock Text="Изменить HTML файл."/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                </controls:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
