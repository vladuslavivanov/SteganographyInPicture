<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SteganographyInPicture.UI.Pages.View.StudyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteganographyInPicture.UI.Pages.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:menuItem="using:SteganographyInPicture.Models"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <NavigationView Grid.Row="0" IsSettingsVisible="False" IsBackButtonVisible="Collapsed" PaneDisplayMode="Top" 
                        MenuItemsSource="{x:Bind ViewModel.StudyItems}">
            
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="ItemInvoked">
                    <core:EventTriggerBehavior.Actions>
                        <core:InvokeCommandAction Command="{x:Bind ViewModel.NavigateCommand}" />
                    </core:EventTriggerBehavior.Actions>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>

            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="menuItem:StudyMenuItemModel">
                    <NavigationViewItem Content="{x:Bind MenuItemName}" Tag="{x:Bind}"/>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>

        </NavigationView>

        <ScrollViewer VerticalScrollBarVisibility="Hidden" Grid.Row="1">

            <Grid>
                <ItemsControl ItemsSource="{x:Bind ViewModel.SelectedItem.Paragraphs, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="menuItem:Paragraph">
                            <controls:SettingsCard ContentAlignment="Left">
                                <StackPanel >
                                    <TextBlock Text="{x:Bind Title}" FontWeight="Bold" TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind Text}" TextWrapping="Wrap" />
                                </StackPanel>
                            </controls:SettingsCard>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical" Spacing="10" Margin="10"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>

            

        </ScrollViewer>
    </Grid>
</Page>
