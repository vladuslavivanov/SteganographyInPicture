<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SteganographyInPicture.UI.Pages.View.EraseImagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteganographyInPicture.UI.Pages.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:converters="using:SteganographyInPicture.Converters"
    xmlns:toolkitConverters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <toolkitConverters:StringVisibilityConverter x:Key="StringVisibilityConverter"/>
        <converters:DoubleToIntConverter x:Key="DoubleToIntConverter"/>
    </Page.Resources>
    
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Spacing="10" Margin="10" HorizontalAlignment="Stretch">
                <controls:SettingsCard ContentAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Spacing="10" HorizontalAlignment="Left">
                            <Button Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.OpenFileCommand}">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <SymbolIcon Symbol="Import"/>
                                    <TextBlock Text="Открыть картинку для очистки"/>
                                </StackPanel>
                            </Button>

                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="Путь к картинке: " VerticalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.PathToImage, Mode=TwoWay}" VerticalAlignment="Center" ToolTipService.ToolTip="{x:Bind ViewModel.PathToImage, Mode=TwoWay}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </controls:SettingsCard>

                <controls:SettingsCard ContentAlignment="Left" Visibility="{x:Bind ViewModel.PathToImage, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}">

                    <StackPanel Orientation="Vertical" Spacing="10">
                        <StackPanel Spacing="10">
                            <TextBlock Text="Глубина очистки"/>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <NumberBox Minimum="0" Maximum="8" SpinButtonPlacementMode="Inline" ValidationMode="InvalidInputOverwritten" Width="200" Value="{x:Bind ViewModel.EncodingDepth, Mode=TwoWay, Converter={StaticResource DoubleToIntConverter}}"/>
                                <FontIcon Glyph="&#xE946;" ToolTipService.ToolTip="Глубина внедрения указывает количество бит, которые будут заменены псевдослучайными битами."/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <Button Content="Очистить" Command="{x:Bind ViewModel.ClearBitsCommand}"/>
                        </StackPanel>
                    </StackPanel>
                    
                </controls:SettingsCard>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
